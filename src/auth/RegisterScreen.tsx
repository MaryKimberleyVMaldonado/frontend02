//import React from 'react';

/**
import '../styles/LoginScreen.css';

import { useAuth } from "../context/AuthContext";
import { useNavigate } from "react-router-dom";

const RegisterScreen: React.FC = () => {
  const { login } = useAuth();
  const navigate = useNavigate();
  
  const handleLogin = () => {
    login();
    navigate("/udashboard");
  };

  return (
    <div className="main">  	
            <input type="checkbox" id="chk" aria-hidden="true" />

            <div className="signup">
                <form>
                    <label htmlFor="chk" aria-hidden="true">Sign up</label>
                    <input type="email" name="email" placeholder="Email" required={true} />
                    <input type="password" name="pswd" placeholder="Password" required={true} />
                    <input type="number" name="type" placeholder="Account Type" required={true} />
                    <button onClick={handleLogin}>Sign up</button>
                </form>
            </div>
        </div>
  );
};

export default RegisterScreen;
 */

/*

                    +<input type="text" name="txt" placeholder="First Name" required={true} />
                    <input type="text" name="txt" placeholder="Last Name" required={true} />
                    <input type="number" name="phone" placeholder="Phone Number" required={true} />
                    <input type="number" name="date" placeholder="Date of birth" required={true} />

*/