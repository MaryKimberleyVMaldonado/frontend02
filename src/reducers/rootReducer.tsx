/**

import { combineReducers } from 'redux';

//import { uiReducer } from './uiReducer';
//import { calendarReducer } from './calendarReducer';

// Define the type for the UI state (replace with actual state type for uiReducer)
interface UIState {
    // Add properties for the UI state here
}

// Define the type for the Calendar state (replace with actual state type for calendarReducer)
/*interface CalendarState {
    // Add properties for the Calendar state here
}

// Combine the reducers
/*
export const rootReducer = combineReducers({
    user: userReducer,
    loan: loanReducer,
    // TODO: AuthReducer
});


// Infer the RootState type from the rootReducer
//export type RootState = ReturnType<typeof rootReducer>;

function rootReducer() {
return ( <div>hi</div> );}

export default rootReducer;

**/